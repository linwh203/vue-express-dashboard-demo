version: "3.9"  # optional since v1.27.0
services:
  web:
    build: ./web
    ports:
      - 80:80
  api:
    build: ./express
    ports:
      - 3001:3001
    environment:
      - DATA_PATH=csv
    